const chai = require('chai'),
    expect = chai.expect,
    chaiAsPromised = require('chai-as-promised');

describe('Api key generation', function () {
    it('should return an object with two properties');
    it('should have a property called id and key, of type string');
});
